module.exports = {
    
    // getUserData: async function (controller, user_id) {
    //     // Access user data from DB
    //     const items = await controller.storage.read([user_id]);
        
    //     const userData = items[user_id] || {}

    //     // TODO: Catch cases when user data is empty

    //     return userData;

    // },

    // storeUserData: async function (controller) {

    // },


    // storeAnswer: async function (controller) {

    // },
}